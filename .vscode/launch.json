{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch CodeLLDB",
            "type": "codelldb",
            "request": "launch",
            "preLaunchTask": "Make",
            "expressions": "native",
            "program": "${workspaceFolder}/bin",
            "stdio": [
                "input.txt",
                null
            ]
        }
    ]
}
